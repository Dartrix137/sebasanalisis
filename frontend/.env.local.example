# 127.0.0.1 y no localhost: en Windows, localhost prueba primero IPv6 (::1),
# uvicorn solo escucha en IPv4, y cada conexion nueva paga ~250 ms de espera.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
